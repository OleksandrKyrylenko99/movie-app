import { DurationMoviePipe } from './duration-movie.pipe';

describe('DurationMoviePipe', () => {
  it('create an instance', () => {
    const pipe = new DurationMoviePipe();
    expect(pipe).toBeTruthy();
  });
});
